### 🔧 Configuration

Before running, create a `core/config.py` file with the following variables:

```python
SECRET_KEY = "your_secret_key_here"
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30
```
